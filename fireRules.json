{
  "rules": {
    ".read": "true",
    ".write": "true",
    "businesses": {
            "$business": {
                "businessID": {

                },
                "name": {
                    ".validate": "newData.isString() && newData.val().length > 2 && newData.val().length <48"
                },
                "address": {
                    ".validate": "newData.isString() && newData.val().lenght < 50"
                }
            }
        }
  }
}